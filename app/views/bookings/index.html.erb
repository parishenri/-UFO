<h1>Bookings#index</h1>
<p>Find me in app/views/bookings/index.html.erb</p>

      <% @bookings.each do |booking| %>
<li>  <%= link_to booking.item.name, item_booking_path(booking.item, booking) %>  </li>
      <% end %>
<ul>
</ul>
